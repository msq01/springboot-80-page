# springboot-80-page

一个最小 Spring Boot 项目，启动后监听 80 端口，并在 `http://localhost/` 显示页面。

## 启动

如果已安装 Maven：

```powershell
mvn spring-boot:run
```

当前机器没有全局 Maven，可以直接运行：

```powershell
powershell -ExecutionPolicy Bypass -File .\run.ps1
```
